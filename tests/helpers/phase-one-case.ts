import {IDBKeyRange,indexedDB}from"fake-indexeddb";import type{Asset}from"../../src/domain/asset/index.js";import{asEntityId,asProjectId}from"../../src/domain/shared/id.js";import{requireIsoTimestamp}from"../../src/domain/shared/time.js";import{createMuseDatabase,type MuseDatabase}from"../../src/db/database.js";import{DirectionCommandService,DirectionService}from"../../src/application/direction/index.js";import{MoodboardService}from"../../src/application/moodboard/index.js";import{ProjectCreationService}from"../../src/application/project/index.js";import{ResearchService}from"../../src/application/research/index.js";
export const gateDatabase=(name:string)=>createMuseDatabase(name,{dependencies:{indexedDB,IDBKeyRange}});const id=(caseNumber:number,value:number)=>asEntityId(`40${String(caseNumber).padStart(6,"0")}-0000-4000-8000-${String(value).padStart(12,"0")}`);const required=<T,>(items:readonly T[],index:number):T=>{const item=items[index];if(item===undefined)throw new Error(`Missing case item ${String(index)}`);return item;};
export async function createLockedDirectionCase(database:MuseDatabase,caseNumber:number,name:string){const projectId=asProjectId(id(caseNumber,1));let sequence=100;const nextId=()=>id(caseNumber,sequence++);const created=await new ProjectCreationService(database,{projectIdFactory:()=>projectId,entityIdFactory:nextId}).create({name,description:`${name}完整创意工作流目标`,projectType:"brand",targetOutputs:["brand_identity"]});const research=await new ResearchService(database,{entityIdFactory:nextId}).run({projectId,briefId:created.briefId,seed:"fixed"});const now=requireIsoTimestamp("2026-07-28T22:00:00.000Z");const assets:Asset[]=[1,2,3,4].map(value=>({id:id(caseNumber,value+10),name:`${name}素材${String(value)}`,type:"image",status:"ready",mimeType:"image/webp",byteSize:100,storageKey:`case/${String(caseNumber)}/${String(value)}`,createdAt:now,updatedAt:now}));await database.table("assets").bulkAdd(assets);const moodboard=await new MoodboardService(database,{entityIdFactory:nextId}).generate({projectId,researchSessionId:research.researchSessionId,availableAssetIds:assets.map(item=>item.id),seed:"fixed"});const moodboardId=required(moodboard.moodboardIds,0);const visualDNAId=required(moodboard.visualDNAIds,0);const directions=await new DirectionService(database,{entityIdFactory:nextId}).generate({projectId,briefId:created.briefId,researchSessionId:research.researchSessionId,moodboardId,visualDNAId,assetIds:assets.map(item=>item.id),constraints:[],seed:"fixed"});const lockedDirectionId=required(directions.directionIds,0);await new DirectionCommandService(database).lock(projectId,lockedDirectionId);return{database,projectId,briefId:created.briefId,researchSessionId:research.researchSessionId,moodboardId,visualDNAId,assets,directionIds:directions.directionIds,lockedDirectionId,nextId};}
