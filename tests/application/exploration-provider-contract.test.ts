import assert from "node:assert/strict"; import test from "node:test";
import { validateExplorationProviderOutput, ExplorationProviderError, type ExplorationProviderInput, type ExplorationVariantCandidate } from "../../src/application/exploration/index.js";
import { asEntityId, asProjectId } from "../../src/domain/shared/id.js"; import { requireIsoTimestamp } from "../../src/domain/shared/time.js";
const id=(n:number)=>asEntityId(`38000000-0000-4000-8000-${String(n).padStart(12,"0")}`); const now=requireIsoTimestamp("2026-07-28T00:00:00.000Z"); const projectId=asProjectId(id(1));
const input={ direction:{ id:id(2),projectId,researchSessionId:id(3),moodboardId:id(4),visualDNAId:id(5),creativeSeedIds:[id(6)],opportunityIds:[id(7)],title:"锁定方向",concept:"同一概念",narrative:"叙事",visualDNA:{keywords:["证据"],principles:["构图"]},advantages:["优势"],risks:["风险"],status:"locked",createdAt:now,updatedAt:now },visualDNA:{id:id(5),projectId,moodboardId:id(4),keywords:["证据"],colorPalette:[{hex:"#111111",role:"主色"}],composition:["网格"],imagery:["纪实"],createdAt:now,updatedAt:now},references:[{id:id(8),projectId,directionId:id(2),assetId:id(9),role:"inspiration",createdAt:now,updatedAt:now}],sourceAssetIds:[id(9)],axes:["composition","color","lighting"],seed:"fixed" } satisfies ExplorationProviderInput;
const variant=(n:number):ExplorationVariantCandidate=>{const label=String(n);return{key:`v${label}`,conceptId:id(2),label:`方案${label}`,axisValues:[{axis:"composition",value:`构图${label}`},{axis:"color",value:`色彩${label}`},{axis:"lighting",value:`光线${label}`}],referenceIds:[id(8)],sourceAssetIds:[id(9)],preview:{fixtureKey:`fixture-${label}`,width:1200,height:900,mimeType:"image/webp"}};};
void test("exploration contract accepts 4–6 multi-axis variants with one concept",()=>{assert.equal(validateExplorationProviderOutput({variants:[1,2,3,4].map(variant)},input).variants.length,4);});
void test("exploration contract rejects count, concept drift and duplicate expression",()=>{assert.throws(()=>validateExplorationProviderOutput({variants:[variant(1)]},input),ExplorationProviderError);assert.throws(()=>validateExplorationProviderOutput({variants:[variant(1),variant(2),{...variant(3),conceptId:id(99)},variant(4)]},input),ExplorationProviderError);assert.throws(()=>validateExplorationProviderOutput({variants:[variant(1),{...variant(2),axisValues:variant(1).axisValues},variant(3),variant(4)]},input),ExplorationProviderError);});
