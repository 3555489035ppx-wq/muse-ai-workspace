import assert from"node:assert/strict";import test from"node:test";import{asEntityId,asProjectId}from"../../src/domain/shared/id.js";import{PromptRationaleError,PromptRationaleService,type StructuredPromptSpec}from"../../src/application/prompt/index.js";
const id=(n:number)=>asEntityId(`43000000-0000-4000-8000-${String(n).padStart(12,"0")}`);const make=(term:string):StructuredPromptSpec=>({projectId:asProjectId(id(1)),explorationId:id(2),directionId:id(3),visualDNAId:id(4),researchSessionId:id(5),fields:{objective:["目标"],composition:[`${term}构图`],color:[`${term}色彩`],material:[`${term}材质`],camera:[`${term}镜头`],lighting:[`${term}光线`],typography:["字体"],imagery:["图像"],constraints:["约束"]},sourceRefs:{objective:[id(6)],composition:[id(3),id(4)],color:[id(4),id(7)],material:[id(3),id(4)],camera:[id(3),id(7)],lighting:[id(4),id(7)],typography:[id(3)],imagery:[id(3),id(4)],constraints:[id(6)]}});
void test("prompt rationale is deterministic and every assertion carries real source IDs",()=>{const service=new PromptRationaleService();const first=service.explain(make("文化"));assert.deepEqual(first,service.explain(make("文化")));assert.equal(first.length,5);assert.equal(first.every(item=>item.sourceIds.length>0&&item.statement.includes(item.sourceIds[0]??"missing")),true);});
void test("prompt rationale rejects missing sources and differs by case evidence",()=>{const service=new PromptRationaleService();const broken=make("文化");assert.throws(()=>service.explain({...broken,sourceRefs:{...broken.sourceRefs,camera:[]}}),PromptRationaleError);assert.notDeepEqual(service.explain(make("文化")),service.explain(make("咖啡")));});
