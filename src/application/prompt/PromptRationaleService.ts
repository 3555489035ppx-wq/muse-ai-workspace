import type{EntityId}from"../../domain/shared/id.js";import type{PromptField,StructuredPromptSpec}from"./contracts.js";
export const RATIONALE_FIELDS=["composition","color","material","camera","lighting"]as const;export interface PromptRationaleAssertion{readonly field:(typeof RATIONALE_FIELDS)[number];readonly statement:string;readonly terms:readonly string[];readonly sourceIds:readonly EntityId[];}export class PromptRationaleError extends Error{constructor(readonly field:PromptField,message:string){super(message);this.name="PromptRationaleError";}}
const LABELS={composition:"构图",color:"颜色",material:"材质",camera:"镜头",lighting:"光线"}as const;
export class PromptRationaleService{explain(spec:StructuredPromptSpec):readonly PromptRationaleAssertion[]{return RATIONALE_FIELDS.map(field=>{const terms=spec.fields[field].filter(item=>item.trim());const sourceIds=spec.sourceRefs[field];if(terms.length===0||sourceIds.length===0)throw new PromptRationaleError(field,`${field} rationale requires traceable terms and source IDs.`);return{field,terms,sourceIds,statement:`${LABELS[field]}采用「${terms.join("、")}」，来源实体：${sourceIds.join("、")}。`};});}}
